# ColorGenerator
CGen is a cross-platform Python tool that generates ANSI color escape codes for multiple programming languages, allowing developers to convert color names or hex values into ready-to-use syntax with live terminal previews.
